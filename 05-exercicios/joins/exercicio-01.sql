-- ============================================
-- Exercícios de JOIN
-- Use o banco criado em: 04-sql-basico/scripts/criacao-tabelas-exemplo.sql
-- Tente resolver antes de ver o gabarito!
-- ============================================

-- 1. Liste todos os pedidos com o nome do cliente (use INNER JOIN).

-- 2. Liste todos os clientes e seus pedidos. Inclua clientes que nunca compraram (use LEFT JOIN).

-- 3. Quais clientes nunca fizeram um pedido?

-- 4. Liste todos os produtos com o nome da categoria.

-- 5. Liste os itens de cada pedido com: nome do cliente, nome do produto, quantidade e preço unitário.

-- 6. Qual o nome do produto mais caro de cada categoria? (Dica: JOIN + GROUP BY + MAX)

-- 7. Liste os pedidos feitos em janeiro de 2024 com o nome do cliente e o total.

-- 8. Quantos produtos existem em cada categoria? (Dica: JOIN + GROUP BY + COUNT)

-- 9. Qual cliente gastou mais no total? Mostre nome e total gasto.

-- 10. Liste os produtos que ainda não foram pedidos em nenhum pedido. (Dica: LEFT JOIN + IS NULL)
